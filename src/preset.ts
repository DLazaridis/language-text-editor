/**
 * Storybook preset for the language-text-editor addon.
 *
 * This addon is manager-only and does not need to augment
 * Vite or Webpack configurations. The file is kept so the
 * addon can be registered via the standard `addons` array.
 */
export {};
